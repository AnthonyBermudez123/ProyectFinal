--Primera Version de la database "TerrorTiket"

-- Tabla de Categorías de Edad
CREATE TABLE CategoriasEdad (
    CategoriaID INT PRIMARY KEY,
    Nombre VARCHAR(50) NOT NULL
);

-- Tabla de Precios
CREATE TABLE Precios (
    PrecioID INT PRIMARY KEY,
    CategoriaID INT,
    Precio DECIMAL(10, 2) NOT NULL,
    FOREIGN KEY (CategoriaID) REFERENCES CategoriasEdad(CategoriaID)
);

-- Tabla de Entradas
CREATE TABLE Entradas (
    EntradaID INT PRIMARY KEY,
    UsuarioID INT,
    CategoriaID INT,
    FechaCompra DATE NOT NULL,
    FOREIGN KEY (UsuarioID) REFERENCES Usuarios(UsuarioID),
    FOREIGN KEY (CategoriaID) REFERENCES CategoriasEdad(CategoriaID)
);

-- Ejemplo de consulta SQL para agregar una entrada comprada por un usuario
INSERT INTO Entradas (UsuarioID, CategoriaID, FechaCompra)
VALUES (1, 2, '2023-10-01');
